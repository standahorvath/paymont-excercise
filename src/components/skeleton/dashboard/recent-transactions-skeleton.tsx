import { TransactionsTableSkeleton } from "@/components/skeleton/transactions/transactions-table-skeleton"

export const RecentTransactionsSkeleton = () => (
  <TransactionsTableSkeleton rows={6} columns={4} />
)
